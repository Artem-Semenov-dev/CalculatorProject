package src.calculator.math;

/**
 *  {@code NumberState} is an enumeration of mathematical token
 *  that used in {@link src.calculator.MathElementResolverFactoryImpl}.
 */

public enum MathElement {

    EXPRESSION,
    NUMBER,
    OPERAND,
    BRACKETS,
    CALCULATOR,
    FUNCTION
}
